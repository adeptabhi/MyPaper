import 'package:flutter/material.dart';

class AppColors {
  static const Color white = Color(0xFFFFFFFF);

  // Red
  static const Color red = Color(0xFFFF3B30);
  static get red50 => red.withValues(alpha: 0.5);
  static get red15 => red.withValues(alpha: 0.15);

  // Blue
  static const Color blue = Color(0xFF1D4380);
  static get blue50 => Color(0xFF1D4380).withValues(alpha: 0.5);
  static get blue15 => blue.withValues(alpha: 0.15);

  // Green
  static const Color green = Color(0xFF299340);
  static get green50 => green.withValues(alpha: 0.5);
  static get green15 => green.withValues(alpha: 0.15);

  // Grey
  static const Color grey = Color(0xFF545454);
  static get grey50 => grey.withValues(alpha: 0.5);
  static get grey15 => grey.withValues(alpha: 0.15);

  // Black
  static const Color black = Color(0xFF000000);
  static get black50 => black.withValues(alpha: 0.5);
  static get black15 => black.withValues(alpha: 0.15);

  // Pink
  static const Color pink = Color(0xFFFF4081);
  static get pink50 => pink.withValues(alpha: 0.5);
  static get pink15 => pink.withValues(alpha: 0.15);

  // Violet
  static const Color violet = Color(0xFF6A1B9A);
  static get violet50 => violet.withValues(alpha: 0.5);
  static get violet15 => violet.withValues(alpha: 0.15);

  // Sky
  static const Color sky = Color(0xFF0277BD);
  static get sky50 => sky.withValues(alpha: 0.5);
  static get sky15 => sky.withValues(alpha: 0.15);

  // Teal
  static const Color teal = Color(0xFF00796B);
  static get teal50 => teal.withValues(alpha: 0.5);
  static get teal15 => teal.withValues(alpha: 0.15);

  // Amber
  static const Color amber = Color(0xFFFF8F00);
  static get amber50 => amber.withValues(alpha: 0.5);
  static get amber15 => amber.withValues(alpha: 0.15);

  // Indigo
  static const Color indigo = Color(0xFF303F9F);
  static get indigo50 => indigo.withValues(alpha: 0.5);
  static get indigo15 => indigo.withValues(alpha: 0.15);

  // Olive
  static const Color olive = Color(0xFF558B2F);
  static get olive50 => olive.withValues(alpha: 0.5);
  static get olive15 => olive.withValues(alpha: 0.15);

  // Charcoal
  static const Color charcoal = Color(0xFF37474F);
  static get charcoal50 => charcoal.withValues(alpha: 0.5);
  static get charcoal15 => charcoal.withValues(alpha: 0.15);

  // Orange
  static const Color orange = Color(0xFFEF6C00);
  static get orange50 => orange.withValues(alpha: 0.5);
  static get orange15 => orange.withValues(alpha: 0.15);

  // Yellow
  static const Color yellow = Color(0xFFFBC02D);
  static get yellow50 => yellow.withValues(alpha: 0.5);
  static get yellow15 => yellow.withValues(alpha: 0.15);
}
